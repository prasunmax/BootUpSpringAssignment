spring:
  cloud:
    config:      
      uri: http://config-server:8000
  application:
    name: product-catalog-service

hibernate:
  ogm:
    datastore:
      database: productAssignment

management:
  security:
    enabled: false 