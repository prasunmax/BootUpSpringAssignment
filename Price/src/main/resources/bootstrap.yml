spring:
  cloud:
    config:      
      uri: http://config-server:8000
  application:
    name: price-service

hibernate:
  ogm:
    datastore:
      database: priceAssignment
      
management:
  security:
    enabled: false 