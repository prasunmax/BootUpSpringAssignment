spring:
  cloud:
    config:      
      uri: http://config-server:8000
  application:
    name: cart-service

hibernate:
  ogm:
    datastore:
      database: priceAssignment
      
management:
  security:
    enabled: false 